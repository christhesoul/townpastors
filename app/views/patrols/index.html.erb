<% @patrols.each do |patrol| %>
	<% @shift = Shift.find_or_create_by_patrol_id_and_pastor_id(patrol.id,current_pastor.id) %>
	<%= form_for(@shift) do |f| %>
		<div class="row">
			<div class="span4"><%= patrol.patrol.strftime("%A %d %B %Y") %></div>
			<div class="span3">
				<label class="radio">
				  <%= f.radio_button "status","available" %>
					Available
				</label>
			</div>
			<div class="span3">
				<label class="radio">
				  <%= f.radio_button "status","unavailable" %>
					Unavailable
				</label>
			</div>
			<div class="span2"><%= f.submit "Update", :class => "btn" %></div>
			<%= f.hidden_field :patrol_id, :value => patrol.id %>
		</div>
	<% end %>
<% end %>